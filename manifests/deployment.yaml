apiVersion: apps/v1
kind: Deployment
metadata:
  name: repcal
  labels:
    app: repcal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: repcal
  template:
    metadata:
      labels:
        app: repcal
      namespace: repcal
    spec:
      containers:
      - name: repcal
        image: tupperward/repcal:v1.2
        imagePullPolicy: Always
        ports:
          - containerPort: 80